# Changelog

Release notes are captured in the GitHub Releases changelog generated by
`.github/workflows/release-main.yml` for each release tag in the format
`<semver>-<yyyy-mm-dd>`.

This repository intentionally does not maintain a manual `Unreleased` section
to avoid drift or duplication with release notes.

Each release publishes:

- Release notes with commit summaries since the previous release tag
- A `CHANGELOG-<release-tag>.md` artifact attached to the release assets
