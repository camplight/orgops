---
type: task.created
---

Agent created a task-like execution context while handling an event.

Payload:

- `inReplyTo` (source event id)
- `eventType` (source event type)
- `toolResultCount` (number of tool results produced)
