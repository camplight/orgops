{
  "name": "@orgops/skills",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "lint": "bun run --silent tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
